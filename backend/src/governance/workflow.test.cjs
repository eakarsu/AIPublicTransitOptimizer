const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const {createWorkflow}=require('./workflowCore.js');const {validateRuntime}=require('./runtime.js');const config=require('./config.js');const workflow=createWorkflow(config);const headers={'x-tenant-id':'tenant-a','idempotency-key':'request-a'};function root(){let p=__dirname;while(p!==path.dirname(p)&&!fs.existsSync(path.join(p,'start.sh')))p=path.dirname(p);return p;}function bad(){const x={...config.acceptedFixture};if('rightsStatus'in x)x.rightsStatus='missing';else if('evidenceFreshnessStatus'in x)x.evidenceFreshnessStatus='stale';else if('injectionStatus'in x)x.injectionStatus='failed';else if('inputReconciliationStatus'in x)x.inputReconciliationStatus='unbalanced';else if('pricingReconciliationStatus'in x)x.pricingReconciliationStatus='unbalanced';else if('consentStatus'in x)x.consentStatus='missing';else if('freshnessStatus'in x)x.freshnessStatus='stale';else x.safetyLimitsVerified=false;return x;}
test('domain topology and typed connectors are declared',()=>{assert.ok(config.states.includes(config.initialState));assert.ok(config.transitions.length>5);assert.ok(config.connectors.length>5);for(const r of config.transitions){assert.ok(config.states.includes(r.from));assert.ok(config.states.includes(r.to));assert.ok(r.roles.length);}});
test('tenant idempotency actor and create roles fail closed',()=>{assert.throws(()=>workflow.context({}, {id:'a',role:config.createRoles[0]}),/X-Tenant-Id/);const c=workflow.context(headers,{id:'creator-a',role:config.createRoles[0]});const record=workflow.createCase({subjectRef:'subject-17',policyVersion:'p1',effectiveAt:'2026-07-18T00:00:00Z',sourceSnapshot:{version:'v1'}},c);assert.equal(record.state,config.initialState);assert.throws(()=>workflow.createCase({subjectRef:'subject-17',policyVersion:'p1',effectiveAt:'2026-07-18T00:00:00Z'},{...c,role:'visitor'}),/Role cannot create/);});
test('evidence is opaque typed and digest verified',()=>{assert.equal(workflow.evidence({kind:config.evidenceKinds[0],sourceRef:'vault:evidence-17',sourceVersion:'v1',sha256:'a'.repeat(64),capturedAt:'2026-07-18T10:00:00Z'}).sha256,'a'.repeat(64));assert.throws(()=>workflow.evidence({kind:config.evidenceKinds[0],sourceRef:'vault:x',sourceVersion:'v1',sha256:'bad',capturedAt:'2026-07-18T10:00:00Z'}),/sha256/);assert.throws(()=>workflow.evidence({kind:config.evidenceKinds[0],sourceRef:'vault:x',sourceVersion:'v1',sha256:'b'.repeat(64),capturedAt:'2026-07-18T10:00:00Z',metadata:{raw_content:'secret'}}),/opaque references/);});
test('versioned acceptance fixture is deterministic and consequential output stays null',()=>{const c=workflow.context(headers,{id:'reviewer-a',role:config.assessmentRoles[0]});assert.equal(workflow.deterministicAssessment({},c).disposition,'insufficient_evidence');const good=workflow.deterministicAssessment(config.acceptedFixture,c);assert.equal(good.disposition,config.readyDisposition);assert.equal(good[config.decisionField],null);assert.equal(good.automatedDecision,false);assert.equal(good.requiresHumanReview,true);assert.equal(workflow.deterministicAssessment(bad(),c).disposition,config.holdDisposition);});
test('transitions enforce evidence versions RBAC and dual control',()=>{const r=config.transitions[0],c=workflow.context(headers,{id:'actor-b',role:r.roles[0]}),rec={state:r.from,version:1,evidenceCount:0,createdBy:'actor-a',lastActorId:null};assert.throws(()=>workflow.transition(rec,{action:r.action,expectedVersion:1,reason:'reviewed authoritative evidence'},c),/evidence/);assert.throws(()=>workflow.transition({...rec,evidenceCount:1},{action:r.action,expectedVersion:2,reason:'reviewed authoritative evidence'},c),/changed/);assert.equal(workflow.transition({...rec,evidenceCount:1},{action:r.action,expectedVersion:1,reason:'reviewed authoritative evidence'},c).to,r.to);const d=config.transitions.find(x=>x.dualControl),dc=workflow.context(headers,{id:'actor-b',role:d.roles[0]});assert.throws(()=>workflow.transition({state:d.from,version:2,evidenceCount:1,createdBy:'actor-a',lastActorId:'actor-b'},{action:d.action,expectedVersion:2,reason:'independent approval complete'},dc),/second person/);});
test('runtime rejects weak secrets wildcard CORS and production legacy flags',()=>{assert.throws(()=>validateRuntime({NODE_ENV:'test',JWT_SECRET:'short'}),/JWT_SECRET/);assert.throws(()=>validateRuntime({NODE_ENV:'production',JWT_SECRET:'x'.repeat(40),DATABASE_URL:'postgres://configured',CORS_ORIGINS:'*'}),/allowlist/);assert.throws(()=>validateRuntime({NODE_ENV:'production',JWT_SECRET:'x'.repeat(40),DATABASE_URL:'postgres://configured',CORS_ORIGINS:'https://client.example',ENABLE_LEGACY_PROVIDER_ROUTES:'true'}),/prohibited/);assert.equal(validateRuntime({NODE_ENV:'test',JWT_SECRET:'x'.repeat(40)}),true);});
test('migration and launcher preserve append-only nondestructive boundaries',()=>{const p=root(),m=fs.readFileSync(path.join(p,'backend/migrations/001_governed_transit_plan.sql'),'utf8'),launch=fs.readFileSync(path.join(p,'start.sh'),'utf8');assert.match(m,/governed_tenant_memberships/);assert.match(m,/append-only/);assert.match(m,new RegExp(config.caseType));assert.doesNotMatch(m,/DROP TABLE|TRUNCATE/);assert.doesNotMatch(launch,/kill\s+-9|pkill|dropdb|createdb|npm\s+install|seed\.js|psql\s+.*-f/);});
