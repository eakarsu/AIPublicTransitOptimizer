function createProviderGate(prefixes){const routes=Object.freeze([...prefixes]);return function(req,res,next){const p=String(req.path||req.originalUrl||'');if(!routes.some(x=>p.startsWith(x)))return next();if(process.env.ENABLE_LEGACY_PROVIDER_ROUTES!=='true')return res.status(503).json({error:'PROVIDER_ROUTE_QUARANTINED',message:'Generated/provider routes require an explicit non-production evaluation gate.'});if(process.env.NODE_ENV==='production')return res.status(503).json({error:'PROVIDER_CONTRACT_UNVERIFIED'});if(!process.env.OPENROUTER_API_KEY)return res.status(503).json({error:'PROVIDER_CREDENTIALS_MISSING'});return next();};}module.exports={createProviderGate};
