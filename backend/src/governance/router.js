const express=require('express');const {sequelize}=require('../models');const {authenticate}=require('../middleware/auth');const {createWorkflow}=require('./workflowCore');const {createGovernedRouter}=require('./routerFactory');const rows=async(executor,s,p)=>{const [r]=await executor.query(s,{bind:p});return Array.isArray(r)?r:[];};const db={query:(s,p)=>rows(sequelize,s,p),transaction:work=>sequelize.transaction(t=>work((s,p)=>rows(t,s,p)))};module.exports=createGovernedRouter({express,workflow:createWorkflow(require('./config')),auth:authenticate,db});
